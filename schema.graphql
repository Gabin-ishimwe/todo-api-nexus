### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Category {
  createdAt: DateTime
  id: Int
  name: String
  tasks: [Task]!
  updatedAt: DateTime
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

type Feed {
  count: Int!
  id: ID
  tasks: [Task!]!
}

type Mutation {
  addCategory(name: String!): Category
  createTask(categoryId: Int!, description: String!, title: String!): Task!
  deleteCategory(id: Int!): Category
  deleteTask(id: Int!): Task!
  markAllDone: Task
  markDone(id: Int!): Task
  updateCategory(id: Int!, name: String!): Category
  updateTask(description: String, id: Int!, title: String): Task
  userLogin(email: String!, password: String!): User
  userRegister(email: String!, firstName: String!, lastName: String!, password: String!): User
}

type Query {
  ReadCategory(id: Int!): Category!
  ReadTask(id: Int!): Task!
  allMarkDone: [Task]!
  allUsers: [User]!
  categories: [Category]!
  feed(orderBy: [SortingTasks!], skip: Int, take: Int): Feed!
}

enum SortInput {
  asc
  desc
}

input SortingTasks {
  createdAt: SortInput
  title: SortInput
}

type Task {
  category: Category!
  createdAt: DateTime
  description: String
  id: Int
  isDone: Boolean
  title: String
  updatedAt: DateTime
  user: User!
}

type User {
  categories: [Category]!
  createdAt: DateTime
  email: String
  firstName: String
  id: Int
  lastName: String
  password: String
  tasks: [Task]!
  token: String
  updatedAt: DateTime
}